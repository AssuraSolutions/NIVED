-- Run the Prisma seeder
-- This script will populate the database with sample data

-- Note: This is a placeholder SQL file
-- The actual seeding is done via the TypeScript seeder at prisma/seed.ts
-- Run: npm run db:seed

SELECT 'Run "npm run db:seed" to populate the database with sample data.' as message;
